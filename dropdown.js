// iterate through each element in const and put each name in an array

// This dropdown will be used for changing the price as well as removing item

import gunkan from './const';

nameArr = sushi.gunkan.map(e => (e.name));
console.log(nameArr);


